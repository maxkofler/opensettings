# OpenSettings

The OpenSettings project aims to centralize configuration of software in one place and provide unified interfaces for them.

The long-term goal is to provide a centralized way of configuring software without the need to tamper with configuration files directly. This simplifies and streamlines the configuration of an operating system by providing a common foundation that users and programs build on to create abstractions.
